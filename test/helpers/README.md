# Test helpers

This package has no WinCC OA project registration helpers.

Use checked-in classic log fixtures under `test/fixtures/`:

- `sample-pvss_ii.log` — small synthetic sample
- `sample-syntax-check-PVSS_II.log` — UI syntax-check style output
- `sample-quick-fail-PVSS_II.log` — early SEVERE / unregistered project
- `sample-test-runner-PVSS_II.log` — CtrlTF test-runner style output
